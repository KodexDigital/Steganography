@* @model List<VisitorLog>

@{
    ViewData["Title"] = "VisitorLogs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>VisitorLogs</h1>

<h2>Visitor Logs</h2>
<table class="table">
    <thead>
        <tr>
            <th>IP</th>
            <th>Browser</th>
            <th>OS</th>
            <th>Device</th>
            <th>City</th>
            <th>Country</th>
            <th>ISP</th>
            <th>Time</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var log in Model)
        {
            <tr>
                <td>@log.IpAddress</td>
                <td>@log.Browser</td>
                <td>@log.OperatingSystem</td>
                <td>@log.Device</td>
                <td>@log.City</td>
                <td>@log.Country</td>
                <td>@log.Isp</td>
                <td>@log.VisitTime.ToLocalTime()</td>
            </tr>
        }
    </tbody>
</table> *@
